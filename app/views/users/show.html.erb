<% the_user = @user.name %>
<% provide(:title, the_user) %>
<div class="row">
	<aside class="col-md-4">
		<section class="user_info">
			<h1>
				<%= gravatar_for @user %>
				<%= the_user %>
			</h1>
		</section>
	</aside>
</div>

		